<% 
  const setting = include('./settings/_app');
%>
<%- include(setting.path.root + '/layouts/templates/_base.ejs', {
  setting,
  view: {
    path: setting.path.root + '/views/_home',
    params: {
      body: null
    }
  }
}); %>
