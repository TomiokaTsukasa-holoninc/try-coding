<% 
  const setting = include('./settings/_app');
%>
<%- include(setting.path.root + '/layouts/_home.ejs', {
  setting,
  view: setting.path.root + '/views/_home',
  params: {
    body: null
  }
}); %>
